twig:
    file_name_pattern: '*.twig'
    paths:
        '%kernel.project_dir%/assets/images': images
        '%kernel.project_dir%/assets/app': global

when@test:
    twig:
        strict_variables: true
